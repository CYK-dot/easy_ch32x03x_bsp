# CH32X035 调试配置 —— 仅调试(GDB server),不烧录
#
# 适用探针: WCH-Link / WCH-LinkE / WCH-LinkW(RISC-V 模式)
# 实测通过: WCH-LinkW mode:RV v2.13 / CH32X035 / sdi transport / GDB server :3333
#
# 说明:
#   - 该配置与 MRS2 官方 bin/wch-riscv.cfg 结构一致(wlinke 驱动 + sdi 传输 + wch_riscv 目标)
#   - 未声明 flash bank: 纯调试场景无需烧录; 烧录请用 WCH-LinkUtility / MRS2 原有方式
#   - init + reset halt: 启动即挂起目标于复位向量,便于设断点后继续执行

adapter driver wlinke
adapter speed 6000
transport select sdi

wlink_set_address 0x00000000

set _CHIPNAME wch_riscv
sdi newtap $_CHIPNAME cpu -irlen 5 -expected-id 0x00001

set _TARGETNAME $_CHIPNAME.cpu

target create $_TARGETNAME.0 wch_riscv -chain-position $_TARGETNAME
$_TARGETNAME.0 configure -work-area-phys 0x20000000 -work-area-size 10000 -work-area-backup 1

# 连接后立即复位并挂起,等待 GDB 设断点
init
reset halt
